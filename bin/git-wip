#!/usr/bin/env ruby

wip_all = ARGV.delete("--all") || ARGV.delete("-a")

message =
  if ARGV.any?
    "WIP: " + ARGV.join(" ")
  else
    "WIP"
  end

system("git", "add", "-A") if wip_all
exec("git", "commit", "--no-verify", "-m", message)
